name: Test
on:
  push:
    branches: [ '🙄' ]
    tags: [ 'tag1', 'tag2', 'tag3', 'tag4' ]

jobs:
  job:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Env
        run: env
      - name: Event data
        run: cat $GITHUB_EVENT_PATH
