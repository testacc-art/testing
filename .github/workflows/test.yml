name: Test
on:
  push:
    branches: [ '🙄' ]

jobs:
  job:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Env
        run: env
      - name: Event data
        run: cat $GITHUB_EVENT_PATH
      - name: timeout
        timeout-minutes: 0
        run: sleep 120
