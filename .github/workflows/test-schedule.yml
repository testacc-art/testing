name: Test schedule
on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  foo:
    name: Foo
    runs-on: ubuntu-latest
    if: secrets.FOO != ''
    steps:
      - run: |
          echo "ok";

  bar:
    name: Bar
    runs-on: ubuntu-latest
    if: secrets.BAR != ''
    steps:
      - run: |
          echo "ok";
